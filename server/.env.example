# Sandbox (api.sandbox.co.in) credentials — SERVER ONLY.
# Copy this file to `server/.env` and fill in your keys. Never commit `.env`.
# These are read only by the Express backend and are never sent to the browser.

SANDBOX_BASE_URL=https://api.sandbox.co.in
SANDBOX_API_KEY=
SANDBOX_API_SECRET=
