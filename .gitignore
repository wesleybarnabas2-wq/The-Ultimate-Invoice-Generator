# Dependencies
node_modules/

# Build output
dist/

# Local SQLite database (created at runtime, contains local bill data)
server/*.db
server/*.db-journal
server/*.db-wal

# Env / editor (server secrets like SANDBOX_API_KEY live in server/.env)
.env
.env.local
server/.env
!.env.example
.vscode/
.DS_Store
*.log
